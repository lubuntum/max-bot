services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: max-schedule-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./users.db:/app/users.db
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge